#include <iostream>//gives cin and cout
#include <fstream>//gives ifstream and ofstream
using namespace std;

int main()
{
    double sum = 0, count = 0, num;
    ifstream inputFile;
    inputFile.open("10-23-2022-data.txt")
    if()
    while(!inputFile.eof())
    {
        total = total + stoi(getline(inputFile, line))
    }
    int avg = total / 10;
    cout << "average:" << avg << endl;
    inputFile.close(); 
    return 0;
}