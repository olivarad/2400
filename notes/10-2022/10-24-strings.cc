/*
.insert(pos,str);
.erase(pos, length);
.length();
.empty();
.find(substr, postion*);
.rfind(substr. position*);
.substr(pos.len);
.replace(pos,length,string);
to.string(num);
stoi(string);
stol(string);
cstring -> array of characters
char s[size];// cstrings allways end in null characer or '\0'
getline(line, length, ending);
strncpy(target, source, length*);
strncat(target, source, length*);
strncmp(s1, s2, length*);
*/