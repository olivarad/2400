#include <iostream>//gives cin and cout
#include <fstream>//gives ifstream and ofstream
using namespace std;

int main()
{
    ifstream inputFile;
    inputFile.open(10-23-2022-streams.c.str())
    inputFile >> name;
    inputFile.close();
    getline(cin, name);
    stoi is stringtointeger
    while(!inputFile.eof()){
        runs until there is nothing left in the file
    }
}
